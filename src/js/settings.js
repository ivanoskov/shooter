import * as THREE from "three";

export const STEPS_PER_FRAME = 5;
export const GRAVITY = 30;
export const playerPos = new THREE.Vector3(0, 0.35, 0);
export const playerColliderRadius = 0.35;
export const playerColliderHeight = 0.65;
